# Security

Do not commit WorkOS API keys, WorkOS cookie secrets, Cloudflare API tokens, D1 encryption keys, `.env` files, DSH profiles, or tenant-state files.

Report security issues privately to the repository owner rather than opening a public issue with credentials or customer data. Revoke any credential that may have been exposed before reporting the incident.
